{
  "name": "watch-next-functions",
  "description": "Cloud Functions proxying all third-party API keys (OpenAI, TMDB, OMDb) so they never ship inside the app binary. Every request is gated by Firebase App Check.",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
